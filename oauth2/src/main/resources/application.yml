spring:
  application:
    name: OAuth2

  datasource:
    url: jdbc:mysql://localhost:3306/employee_system
    username: root
    password: 16042002

  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  security:
    oauth2:
      client:

        registration:
          facebook:
            clientId:
            clientSecret:
